const question = document.getElementById("question-cointainer");
const btn = document.getElementById("start-btn");

function myFunction() {
    if (question.display.style === "none") {
        question.display.style = "block";
    }
    
}